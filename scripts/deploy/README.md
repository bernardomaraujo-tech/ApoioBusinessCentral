# Deploy scripts

Adicionar aqui scripts de deploy quando a infraestrutura estiver fechada.

Sugestões:

- deploy Integration API;
- deploy workers;
- configurar variáveis de ambiente;
- validar healthcheck;
- executar smoke tests.
