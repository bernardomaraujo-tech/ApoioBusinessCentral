name: Release

on:
  workflow_dispatch:

jobs:
  release-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show release checklist
        run: cat RELEASE-CHECKLIST.md
